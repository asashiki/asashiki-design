通用卡片 / 面板。靠 1px 边框分层，raised 才加阴影。

```jsx
<Card raised deco>
  <div className="ad-card__title">标题</div>
  <div className="ad-card__sub">副标</div>
</Card>
```

raised 阴影 · interactive 可点 hover · deco 右上 −12° 斜纹 · glow 顶部柔光。内部可用 ad-card__title / __sub / __body。